# Template Pattern

Purpose : - to provide a method template and deferring some steps of implementation or abstract methods
          - to define a skeleton of an algorithm in a base class and let subclasses override the steps without changing the overall algorithm’s structure.


PS:
 - it is an abstract class definition with abstract and normal methods
 - the template method set to be final so that it cannot be overridden.