# Adapter pattern

Purpose : to make it work two unrelated/incompatible interfaces together.
          This pattern acts as a connector between two interfaces

PS:
 - Adapter class is a wrapper class of the other incompatible object, provide compatible interface

